local function literal(ch)

end

local function either(r, s)

end

local function chain(r, s)

end

local function star(r)

end

return {
   literal = literal,
   either = either,
   chain = chain,
   star = star,
}
